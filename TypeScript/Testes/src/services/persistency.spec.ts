describe('Testando alguma coisa', () => {
  it('should return one (IT)', () => {
    const number = 1;
    expect(number).toBe(1);
  });
  test('should return giorno giovanna (TEST)', () => {
    const name = 'giorno giovanna';
    expect(name).toBe('giorno giovanna');
  });
});
